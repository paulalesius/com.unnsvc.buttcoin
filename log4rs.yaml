refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
  entries:
    kind: file
    path: "target/buttcoin.log"
    encoder:
      pattern: "{d} - {m}{n}"
root:
  level: warn
  appenders:
    - stdout
    - entries
loggers:
  buttcoin:
    level: info

